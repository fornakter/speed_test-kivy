MainWidget:

<MainWidget>:
    orientation: 'vertical'
    Label:
        text: "Speed test"


    Label:
        #id: status_label
        text: root.status_label
    Label:
        text: root.server_label
    Label:
    BoxLayout:
        Label:
        Button:
            text: "Start"
            on_press:
                root.on_press()
                root.on_release()
            on_release:
                root.download_test()
                root.upload_test()
        Label:

    BoxLayout:
        Label:
            text: root.download_label
        Label:
            text: root.upload_label
    Label:

